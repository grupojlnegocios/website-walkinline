import { ContainerCircle, Loading, Circle } from './style.styles'

export const Loader = () => {
  return (
    <ContainerCircle>
      <Loading>
        <Circle />
      </Loading>
    </ContainerCircle>
  )
}
